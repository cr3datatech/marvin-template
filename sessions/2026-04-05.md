# Session Log: 2026-04-05

## Session: 08:22

### Topics
- Prompt caching behaviour — how Anthropic caches the system prompt after the first API call within a 5-minute window at 10% token cost
- Slack bot system prompt lifecycle — confirmed it's built once at bot startup (`__init__`), stored as `self.system_prompt`, and reused for every message
- Two-layer staleness: bot memory (state files frozen at startup) + Anthropic cache (frozen for 5 min after first call)
- Conversation history is always fresh (pulled from SQLite per message, last 10 turns)
- No uncommitted changes — only log files in untracked

### Decisions
- User will test the caching/staleness behaviour directly
- Potential improvement identified: add a `/reload` command to refresh `self.system_prompt` without restarting the bot

### Open Threads
- Test prompt caching behaviour in Slack bot to verify staleness in practice
- `/reload` command for Slack bot not yet implemented

### Next Actions
- Implement `/reload` command in Slack bot if testing confirms staleness is a problem
